name: NodeJS with Webpack

on:
  push:
    branches: [ "main,-netfly" ]
  pull_request:
    branches: [ "main,-netfly" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]

    steps:
    - uses: actions/checkout@v4

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}

    - name: Build
      run: |
        npm install
        npx webpack
            - نام: ServiceNow CI/CD اعمال تغییرات
  # می‌توانید دقیقاً به کامیت یا نسخه پین کنید.
  # کاربردها: ServiceNow/sncicd-apply-changes@d9e560381b65fc450f8dc85b084c1a6cc40e503c
  موارد استفاده: ServiceNow/sncicd-apply-changes@2.0.0
          
